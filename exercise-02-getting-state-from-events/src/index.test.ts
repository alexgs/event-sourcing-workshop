import { expect } from 'chai';
import { getShoppingCart } from './index';

describe('Function `getShoppingCart`', () => {
  it.skip('returns the correct shopping cart ID');

  it.skip('returns the correct user ID');

  it.skip('returns the correct number of items');

  it.skip('returns the correct list of items');
});
